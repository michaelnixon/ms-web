<h1>Listing categories</h1>
<p><em>These are the top-level categories that items can belong to.</em></p>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= link_to raw("<span class='glyphicon glyphicon-edit'></span>"), [:edit, :admin, category] %><%= link_to raw("<span class='glyphicon glyphicon-trash'></span>"), [:admin, category], method: :delete, data: { confirm: 'Are you sure?' }%> <%= category.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Create a new Category', new_admin_category_path %><br/>
<%= link_to raw("<span class='glyphicon glyphicon-arrow-left'>Back</span>"), admin_path %>