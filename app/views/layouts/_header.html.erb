<div class="container-fluid logo-block">
	<%= image_tag("mslogo_01.png", height: "120", alt: "Moving Stories project logo", title: "Moving Stories project logo")%>
	<span class="pull-right social-media"><%= mail_to "admin@movingstories.ca", raw('<i class="fa fa-envelope fa-3x"></i>') %> <a href="https://www.facebook.com/groups/430082843697624/"><i class="fa fa-facebook fa-3x"></a></i> <a href="https://twitter.com/Moving_Stories"><i class="fa fa-twitter fa-3x"></i></a></span>
</div>
<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
	    <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
      </button>
		</div>
		<div class="collapse navbar-collapse" id="navbarCollapse">
	    <ul class="nav navbar-nav nav-items">
	      <li class="active"><a href="" data-filter="*" class="nav_elem">All</a></li>
				<% @promoted_items.each do |item| %>
					<li><a href="#" class="nav_elem promoted" data-item-id="<%= item.id %>"><%= item.name %></a></li>
				<% end %>
	      <% @categories.each do |category| %>
					<li><a href="#" data-filter=".category<%=category.id%>" data-id="<%=category.id%>" class="nav_elem"><%= category.name %></a></li>
				<% end %>
	    </ul>
	  	<form class="navbar-form navbar-right" role="search">
	        <div class="form-group">
	          <input type="text" class="form-control" placeholder="Search" id="search" name="search"/>
		      </div>
	        <button type="submit" class="btn btn-default">Submit</button>
	    </form>
			<ul class="nav navbar-nav navbar-right">
				<% if current_user %>
        <li class="dropdown">
          <a href="#" data-toggle="dropdown" class="dropdown-toggle">
            <span class="glyphicon glyphicon-user"></span> <%= current_user.email %> <b class="caret"></b>
          </a>
          <ul class="dropdown-menu" role="menu">
						<li><%= link_to link_html("dashboard", "Dashboard"), "/admin" %></li>
            <li class="divider"></li>
            <li><%= link_to link_html("log-out", "Logout"), log_out_path %></li>
          </ul>
        </li>
				<% else %>
					<li class="dropdown">
						<%= link_to login_link_html("Login"), log_in_path %>
					</li>
				<% end %>
      </ul>	
    </div>
		<div class="collapse navbar-collapse secondary container-fluid" id="subnavbar">
			<%= render "layouts/secondary_nav"%>
		</div>
</nav>
